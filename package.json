{
  "name": "mern_project",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "frontend": "npm --prefix frontend run dev",
    "backend": "npm --prefix backend run start",
    "start": "npx concurrently \"npm --prefix backend run start\" \"npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
